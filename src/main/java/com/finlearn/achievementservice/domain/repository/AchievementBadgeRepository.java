package com.finlearn.achievementservice.domain.repository;

import com.finlearn.achievementservice.domain.AchievementBadge;

public interface AchievementBadgeRepository {
    AchievementBadge save(AchievementBadge badge);
}
